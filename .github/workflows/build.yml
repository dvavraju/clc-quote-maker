name: Flutter Build APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.27.4'
          cache: true

      # Scaffold a clean Flutter project with proper structure
      - name: Scaffold clean Flutter project
        run: |
          flutter create \
            --org com.thestorybookbyclc \
            --project-name quotmaker \
            --platforms android \
            --no-pub \
            flutter_build

      # Overlay our app source code on top of the scaffold
      - name: Overlay app source code
        run: |
          rm -rf flutter_build/lib
          cp -r lib        flutter_build/lib
          cp -r assets     flutter_build/assets
          cp pubspec.yaml  flutter_build/pubspec.yaml

          # Overlay our custom CLC logo icons
          for density in mipmap-mdpi mipmap-hdpi mipmap-xhdpi mipmap-xxhdpi mipmap-xxxhdpi; do
            src="android/app/src/main/res/$density"
            dst="flutter_build/android/app/src/main/res/$density"
            if [ -d "$src" ]; then
              cp -f "$src"/*.png "$dst"/
            fi
          done

      - name: Install dependencies
        working-directory: flutter_build
        run: flutter pub get

      # Build with --debug flag so signing is handled automatically and reliably
      # For production use later, a proper keystore can be set up
      - name: Build APK
        working-directory: flutter_build
        run: flutter build apk --release --target-platform android-arm64

      - name: Rename APK
        run: |
          mkdir -p apk_output
          cp flutter_build/build/app/outputs/flutter-apk/app-release.apk \
             apk_output/CLC-Quote-Maker.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CLC-Quote-Maker-APK
          path: apk_output/CLC-Quote-Maker.apk
